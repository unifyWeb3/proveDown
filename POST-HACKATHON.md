# POST-HACKATHON.md — Startup Path After Sep 17 (2026-09-05)

Assumption: no fundraising until V2 validation holds. Founder time is the budget.

## Week 1 (Sep 18–25)

Ship submission follow-through: public repo hygiene, demo video + writeup, panel/community responses, winners announced Sep 25 (FACT per portal schedule). Run 5 pipeline-owner interviews (kill criterion #1). Log all in DECISION-LOG. Freeze contract unless reset forces re-deploy.

## Month 1 (to mid-Oct)

Close 2 design partners (free attested spot-checks for logo + case study — `final-product.md:16` channel plan retained). Build poller v1 + wire real bundles through unchanged jury. Implement SLO presets/versioning + real appeal. Instrument PRODUCT-METRICS from day one (resolved/inconclusive/no_consensus/latency/cost/query counts). Legal: terms clarifying attestation is evidence, not legal judgment; testnet-key rotation note resolved before any mainnet key exists; no regulated claims (not insurance, not escrow) until licensed/partnered.

## Month 3 (to mid-Dec)

Verdict on wedge: if metrics validate → price ($99/$499) + webhooks + reputation API + first paid conversions; if kill thresholds trigger → pivot review to procurement verifier (same jury infra, invoice prompt, accounting-firm channel — `08-procurement-vs-sla.md` preserved as fallback, not parallel build). Relay to Base only on partner pull. Open-source strategy: contract + SDK open (integration moat needs trust), poller/eval-tuning private (iteration moat). Security: prompt-hardening pass against live adversarial bundles; bond sizing note for >$100k disputes (scaled bond + fast finality, `final-product.md:24` #5).

## Month 6 (to Mar 2027)

Either (a) V2 commercial: Merkle batch, VerdictRegistry consumers, 10 weekly-attesting customers, margin-positive — then hire/integrate; or (b) disciplined wind-down/pivot with evidence (kill log public in DECISION-LOG). No zombie mode. Ecosystem: GenLayer mainnet path (Clarke/mainnet per genlayer.com roadmap FACT — track as dependency, never promise dates), Internet Court/escrow partnership for SLA-fact calls, inference-marketplace integration (Antseed live in track ecosystem FACT — natural Layer-8 consumer).

## Year 1 (to Sep 2027)

If V2 holds: multi-verifier platform + verified-service marketplace + x402 conditions. If not: extracted assets (jury pattern, reputation math, eval harness) redeployed to the validated wedge. Fundraising only if distribution pull exceeds founder capacity (partners asking faster than we can integrate) — not for runway theater.
